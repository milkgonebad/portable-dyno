<li><%= event.name %> - <%= event.event_start_date %>
    <%= link_to "Edit", edit_event_path(event) %> 
    <%= link_to "Delete", event_path(event), :method => :delete, :confirm => "Are you sure you want to delete this event?" %>
</li>
